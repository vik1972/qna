$('.question-errors').html('<%= render 'shared/errors_html', resource: @question %>');

<% unless @question.errors.present? %>
  $('#question_'+ '<%= @question.id %>').replaceWith('<%= j render @question %>');
<% end %>