- if current_user&.subscribed_of?(resource)
  p= link_to "Unsubscribe", subscription_path(@subscription), method: :delete,  remote: true
- else
  p= link_to "Subscribe", question_subscriptions_path(resource), method: :post,  remote: true